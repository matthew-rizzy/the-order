# Council Deliberation — 2026-06-19 (Fri)

**Trigger:** apprentice invoked `archmage next`. Review Gate check: `date +%F` → 2026-06-19. Both cards in `reviews.md` (procedure vs process; process space cost O(n)/O(1)+TCO) have `next_due = 2026-06-19` → **DUE**. First true spaced test, 1d rung.

## Candidate move (pre-council)
Open with cold blank-page retrieval of both due cards, THEN execute the parked Move #2 closing rep in one session (factorial two ways + substitution trace + predict-check + SICP ex. 1.9–1.11).

## Council verdicts (spawned in parallel)

- **Sage of Method — adjust.** Cold retrieval correctly sequenced before new material; it's the first real spaced test, honor it as a clean no-peek diagnostic. But don't stack the full 1.9–1.11 rep on top — overload risk past the ~40–60% working-memory failure edge. Let the recall miss drive what comes next; 1.10 (Ackermann) is a known overload spike. Defer the heavy build / pick one exercise.

- **Cartographer — adjust.** Mis-sectioning would not survive review: only **1.9 is §1.2.1** (linear recursion vs iteration; deliverable = the substitution trace, not a running program — the true closer, matches the parked rep). **1.10 is §1.2.3** (orders of growth / Ackermann — needs Θ-notation framing not yet taught; park it). **1.11 is the §1.2.2 tree-recursion bridge** (write f both recursively and iteratively). Apprentice fully prepped for 1.9 & 1.11; factorial recursive trace already done live, so fold it into 1.9 and keep the predict-first iterative half as the new rigor. Sequence: cards → factorial(iterative, predict-first) → 1.9 → 1.11; park 1.10; next flagship = §1.2.2 counting-change.

- **Master of the Order — veto.** Friday **is** club night (lore template). A peak evening study block violates rest discipline; also the closing rep must land before any Gate attempt, so the Gate of the First Lamp is not attemptable this session regardless. Cold-retrieve of the two cards Friday is fine (retrieval is trough-safe / light, club-night front-load OK — not novel hard theory). Move the heavy closing rep to the next protected peak slot. Name the Gate of the First Lamp explicitly as the next horizon, earned only after 1.9–1.11 lands clean.

## Reconciliation (Archmage)
Convergent. Split the parked move:
- **Tonight (Fri, club night):** issue retrieval-only — the two due cards, cold blank page, ~6 min, no code. Honors Review Gate + Master's veto + Sage's overload caution simultaneously.
- **Move #2b → next weekday peak (~Mon 06-22), gated on 2a clean:** factorial two ways + substitution trace + predict-check, then **1.9 → 1.11**, **1.10 parked** for the §1.2.3 block (Cartographer's re-scope).
- Schedule note: progress.md records "Sunday off" (apprentice's own preference) — overrides the lore template's "Sunday = biggest study day." So heavy rep lands Monday evening, the recurring weekday slot.
- Predictions SEALED in the issued move text (no leaked answers).
- Gate of the First Lamp named as horizon; not attemptable until 2b lands.

## Issued
Move #2a — "The First Tolling of the Clock." Retrieval-only. THE RETURN: per-card answer + signal (cold/hesitant/peeked) → reschedules each card, decides whether 2b proceeds to generation or re-drills the gap.

**Cards not yet rescheduled** — they are tested by this move; reschedule on the apprentice's report next loop.
