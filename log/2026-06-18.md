# 2026-06-18 — Session Report & Repair

## Move #1 reported (Rank 0 · The First Lamp)
**Watched:** SICP Lecture 1B (and into the 1A/1B material). Note: apprentice may have gone out of order — sent back to catch 1A's opening ~10 min for the sorcerer's-spirit metaphor.
**Pre-write (cold):** "A computational process takes input or not, has output or not, may trigger external functions; a series of steps."
**Post-lecture:** circling "series of steps given input/output"; **procedure vs process unclear** ("maybe I didn't interpret well enough").
**Mastery signal (honest):** no peeking — but the procedure-vs-process distinction did **not** land cold. Not yet a load-bearing stone.

## Archmage assessment (Council lens)
- **Master of the Order:** Gate not passed — hold position, do not advance to new lectures. Advance only by passing, never by time.
- **Sage of Method:** comprehension gap → repair via generation now (re-state the distinction cold), then space it at 1 day (reset, since it didn't land).

## Move #1.5 issued — Repair the distinction
Taught crisply: **procedure = static recipe/spell (the rules)**, **process = the dynamic activity in time the machine performs (the spirit conjured)**; same procedure → different-shaped processes (recursive vs iterative). Clarified "closed-book" = unaided *recall*, not "don't read the text" (textbook reading encouraged, Ch 1–2).
**Asked:** re-state cold, one sentence each — "A procedure is ___. A process is ___." + confirm 1A watched.
**Review card added:** `procedure vs process` → next_due 2026-06-19 (rung 1d).
**Result:** Re-stated cold, unaided — *"A procedure is a list of steps to perform; a process is the actual steps being performed."* ✓ Distinction landed (static vs dynamic). Stone mortared → Mastery Ledger. Card kept at rung 1d (due 06-19) for a genuine spaced test (today's recall was primed by the teaching).

## Move #2 issued — "Stress the Procedure, Trust the Process" (SICP §1.2.1)
**Council:** Cartographer *endorse* — this is §1.2.1 "Linear Recursion and Iteration," correct next step; use DrRacket + `#lang sicp`. Sage *adjust* — make it prediction-first (predict which factorial balloons before coding) + a cold retrieval hook before opening Scheme.
**Note (system):** the Cartographer's domain doc (`course-audit.md`) covers DS&A only, not the SICP/Rank-0 path — it flew on general knowledge. Consider adding a short SICP section to that council doc so Rank 0 has audited coverage.
**The move:** retrieval hook → predict → DrRacket(`#lang sicp`) → write `factorial` recursive + iterative → substitution-model trace both → check vs prediction → SICP ex. 1.9–1.11.
**Apprentice koan minted:** *"Stress the procedure, trust the process."*
**Status:** issued; awaiting report (which ballooned + why + signal).

## Side-insight (mid-Move-#2, apprentice-driven)
Apprentice asked the OS-level question unprompted: *what's the actual computational difference between a ballooning and a flat process?* Walked through the **call stack**: recursive process holds a growing stack of deferred operations → **O(n) space**; iterative process summarizes progress in a few **state variables** → **O(1) space**; both O(n) time; **tail-call optimization** is what lets the iterative version reuse one frame (and why Python/Java without TCO still balloon). Apprentice then derived it back: *"it's only holding the state variable vs the entire pending operations"* and self-corrected procedure-vs-process. Two cards minted (procedure-vs-process already live; **process space cost** new) — both due 06-19 for cold confirmation. Strong sign: he's reaching past the lecture into the machine model on his own.

## Move #2 implementation (apprentice)
Wrote BOTH versions correctly in `#lang sicp`:
- **Tail/flat:** `(define (factorial x r) (if (> x 1) (factorial (- x 1) (* x r)) r))` — correct accumulator, did this one FIRST.
- **Recursive/balloon:** `(define (factorial x) (if (> x 1) (* x (factorial (- x 1))) 1))` — correct.
**Metacognition (apprentice-noticed):** the balloon took *longer* — he'd just internalized the accumulator schema, so "return 1" felt wrong ("I didn't want it to just return 1"). Named it: **Einstellung effect / mental set + proactive interference**, reframed as **interleaving working** (the productive struggle that builds discrimination). Recovered the base-case insight: `1` is the seed/identity the deferred `*`s consume on the unwind, not "the answer."
**Syntax stumbles cleared:** Lisp calls are `(f x)` not `f(x)`; `(lambda (x) …)` needs param parens; `define`-shorthand body goes after the param list; `trace` usage = `(require racket/trace)` → `(trace factorial)` → `(factorial 3)`.
**Status:** both implemented + predicted correctly; trace + ex 1.9–1.11 pending (the visual confirmation).

## Trace seen — the payoff (DrRacket #lang racket + racket/trace)
After clearing the Beginning-Student/#lang trap (set DrRacket to "The Racket Language"), apprentice traced BOTH and read the visuals correctly:
- **balloon:** indentation deepens to depth 3 (3 frames alive at once → O(n)), then **three** `<` returns (answer built on the unwind).
- **flat:** all calls at the **same margin** (each replaces the last → O(1)), then **one** `<` return (answer already in the accumulator).
Apprentice's own words: *"calling it three times flat instead of nesting the whole thing into one call."* — i.e. each tail call finishes-and-replaces vs opens-and-waits. **Procedure ≠ process and O(n) ≠ O(1), seen empirically.** Asked good meta-Qs: trace = entry/exit instrumentation; only `factorial` traced because only it was `(trace)`'d; can't trace core primitives, only named/defined functions. Cards (procedure-vs-process, process-space-cost) now empirically grounded; cold spaced test still 06-19.
**Remaining to close Move #2:** ex 1.9–1.11 (predict recursive-vs-iterative for each).
